services:
    engine:
        build:
            context: .
            dockerfile: Dockerfile.dev
        stdin_open: true
        tty: true
        ports:
            - "8080:8080"
        volumes:
            - .:/app
